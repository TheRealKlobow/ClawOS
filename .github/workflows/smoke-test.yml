name: smoke-test
on:
  workflow_dispatch:
jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: chmod +x tests/smoke/*.sh scripts/smoke-test.sh
      - run: bash scripts/smoke-test.sh
